# GTerm Release Guide - Auto-Update

Пълно ръководство за създаване на нов release с работещ auto-update.

---

## 🚀 Бърз старт

За да пуснете нова версия (напр. 1.0.4):

```bash
./scripts/release.sh 1.0.4
```

Това е! Скриптът автоматично прави всичко необходимо. ✨

---

## 📋 Какво прави скриптът?

Скриптът `release.sh` автоматично:

1. ✅ Обновява версията в `package.json`, `tauri.conf.json`, и `Cargo.toml`
2. ✅ Commit-ва промените
3. ✅ Build-ва апликацията (5-10 минути)
4. ✅ Създава `GTerm.app.tar.gz` архив
5. ✅ Подписва архива с вашия signing key
6. ✅ Генерира `latest.json` файл
7. ✅ Създава GitHub release с tag `v1.0.4`
8. ✅ Upload-ва всички необходими файлове:
   - `GTerm_1.0.4_aarch64.dmg` - за ръчно инсталиране
   - `GTerm.app.tar.gz` - за auto-update
   - `GTerm.app.tar.gz.sig` - signature за верификация
   - `latest.json` - update manifest

---

## 🔑 Signing Keys

### Къде са ключовете?

- **Private key:** `~/.tauri/gterm.key` ⚠️ **ПАЗИ ГО В ТАЙНА!**
- **Public key:** `~/.tauri/gterm.key.pub` (вече е в `tauri.conf.json`)

### ⚠️ ВАЖНО за Private Key

- **НИКОГА** не го commit-вай в Git
- **НИКОГА** не го споделяй с никого
- Направи backup на сигурно място
- Ако загубиш този ключ, няма да можеш да правиш нови updates!

### Ако загубиш ключа

Ако загубиш private key-а, ще трябва да:
1. Генерираш нов ключ
2. Обновиш public key в `tauri.conf.json`
3. Пуснеш нов release
4. Потребителите ще трябва да инсталират ръчно новата версия

---

## 📝 Стъпка-по-стъпка процес

### Преди релиза

1. Убеди се че всички промени са commit-нати
2. Тествай апликацията локално
3. Провери че `gh` CLI е инсталиран и конфигуриран

### Пускане на релиз

```bash
# Пусни нова версия
./scripts/release.sh 1.0.4

# Скриптът ще те попита за потвърждение
# Натисни 'y' за да продължиш
```

### След релиза

1. Отиди на GitHub Release страницата
2. Редактирай release notes (добави какво е ново)
3. Тествай дали update-ът работи:
   - Инсталирай предишната версия
   - Стартирай апликацията
   - Трябва да видиш update notification

---

## 🧪 Тестване на Auto-Update

### Как да тестваш дали update-ът работи?

1. **Инсталирай старата версия** (напр. 1.0.3):
   ```bash
   # Download старата .dmg от GitHub Releases
   # Инсталирай я
   ```

2. **Стартирай апликацията**
   - При стартиране се проверява за update
   - Трябва да видиш dialog: "Update to 1.0.4 is available!"

3. **Натисни "Update Now"**
   - Апликацията изтегля новата версия
   - Инсталира се автоматично
   - Рестартира се

4. **Провери версията**
   - Отвори About/Settings и виж версията
   - Трябва да е 1.0.4

### Debug на auto-update

Ако update-ът не работи, провери:

1. **latest.json е качен на GitHub:**
   ```bash
   curl https://github.com/g0shk3/GTerm/releases/latest/download/latest.json
   ```

2. **Signature е правилен:**
   - Провери че `.sig` файлът е качен
   - Провери че signature в `latest.json` съвпада

3. **Public key е правилен в tauri.conf.json:**
   ```bash
   cat ~/.tauri/gterm.key.pub
   # Сравни със стойността в tauri.conf.json
   ```

4. **URL в latest.json е правилен:**
   - URL трябва да сочи към реалния файл в GitHub Release

---

## 🔧 Ръчен процес (ако не искаш да ползваш скрипта)

### 1. Обнови версията

Промени версията в **3 файла**:

**package.json:**
```json
{
  "version": "1.0.4"
}
```

**src-tauri/tauri.conf.json:**
```json
{
  "version": "1.0.4"
}
```

**src-tauri/Cargo.toml:**
```toml
[package]
version = "1.0.4"
```

### 2. Build

```bash
npm run tauri:build
```

### 3. Създай update package

```bash
cd src-tauri/target/release/bundle/macos
tar -czf GTerm.app.tar.gz GTerm.app
```

### 4. Подпиши

```bash
tauri signer sign GTerm.app.tar.gz --private-key-path ~/.tauri/gterm.key
# Натисни Enter за парола (няма парола)
```

### 5. Генерирай latest.json

```bash
SIGNATURE=$(cat GTerm.app.tar.gz.sig)

cat > latest.json << EOF
{
  "version": "1.0.4",
  "notes": "Bug fixes and improvements",
  "pub_date": "$(date -u +"%Y-%m-%dT%H:%M:%SZ")",
  "platforms": {
    "darwin-aarch64": {
      "signature": "${SIGNATURE}",
      "url": "https://github.com/g0shk3/GTerm/releases/download/v1.0.4/GTerm.app.tar.gz"
    }
  }
}
EOF
```

### 6. Създай GitHub Release

```bash
cd /path/to/GTerm

# Commit и tag
git add .
git commit -m "Release v1.0.4"
git push
git tag v1.0.4
git push origin v1.0.4

# Създай release
gh release create v1.0.4 \
  --title "Release v1.0.4" \
  --notes "Bug fixes and improvements"

# Upload файлове
gh release upload v1.0.4 \
  src-tauri/target/release/bundle/macos/GTerm.app.tar.gz \
  src-tauri/target/release/bundle/macos/GTerm.app.tar.gz.sig \
  src-tauri/target/release/bundle/macos/latest.json \
  src-tauri/target/release/bundle/dmg/GTerm_1.0.4_aarch64.dmg
```

---

## ❓ Често срещани проблеми

### "Wrong password for that key"
- Private key-ят няма парола
- Просто натисни Enter когато те попита

### "File not found"
- Убеди се че build-ът е завършил успешно
- Провери че си в правилната директория

### Update не се показва в апликацията
- Провери че `latest.json` е качен на GitHub
- Провери че URL-ът е правилен
- Провери че версията в `latest.json` е по-висока от текущата

### Signature verification failed
- Провери че използваш същия private key
- Провери че public key в `tauri.conf.json` е правилен
- Regenerate-ни `.sig` файла

### Build грешка
```bash
# Изтрий target директорията и build-ни отново
rm -rf src-tauri/target
npm run tauri:build
```

---

## 📚 Допълнителна информация

### Архитектура на Auto-Update

```
App (v1.0.3)
    ↓
Проверка за update при стартиране
    ↓
Изтегля: https://github.com/.../latest.json
    ↓
Сравнява версиите (1.0.3 vs 1.0.4)
    ↓
Показва dialog на потребителя
    ↓
Изтегля GTerm.app.tar.gz
    ↓
Верифицира signature с public key
    ↓
Инсталира и рестартира
```

### Структура на latest.json

```json
{
  "version": "1.0.4",               // Нова версия
  "notes": "Update notes",          // Какво е ново
  "pub_date": "2025-01-14T...",    // Дата на release
  "platforms": {
    "darwin-aarch64": {             // macOS ARM (M1/M2/M3)
      "signature": "...",           // Signature за верификация
      "url": "https://..."          // URL към .tar.gz файла
    }
  }
}
```

---

## 🎯 Best Practices

1. **Винаги тествай update-а** преди да го пуснеш публично
2. **Пиши ясни release notes** - обясни какво е ново
3. **Използвай semantic versioning** (MAJOR.MINOR.PATCH)
4. **Backup-вай private key-а** на сигурно място
5. **Провери всички файлове** преди upload
6. **Commit промените** преди build

---

## 📞 Поддръжка

Ако имаш проблеми:
1. Провери грешките в конзолата (DevTools)
2. Провери `latest.json` URL-а
3. Провери GitHub Release файловете
4. Regenerate-ни signing key ако е необходимо

---

**Версия на гида:** 2.0.0
**Последна промяна:** 2025-01-14
